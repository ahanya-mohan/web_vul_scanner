"""web_vul_scanner: an educational web vulnerability scanner.

The scanner only runs against hosts that have been explicitly authorized
(see :mod:`web_vul_scanner.core.authorization`). It ships with a deliberately
vulnerable target application under ``targets/`` for safe, reproducible use.
"""

__version__ = "0.1.0"
